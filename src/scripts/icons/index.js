/*
 *
 * ctx — Контекст canvas
 *
 * dot={x, y, r}:
 *   • (x; y) — координаты центра
 *   • r — половина длины стороны хитбокса
 *
 * mode=[fill|stroke]:
 *   • fill — при наведении
 *   • stroke — по умолчанию
 *
 * color — цвет ветки точки
 *
 */

import end from "./end";

export default {
	end
}